import React from 'react';
import {
    StyleSheet,
} from 'react-native';


export default style = StyleSheet.create({
    loginText: {
        color: '#FEFFDE'
    },
    input: {
        border: none,
        borderRadius: '20px',
        padding: '10px 30px'
    }
});